id: display_pdu2char
label: pdu2char
category: '[display]'

templates:
  imports: import display
  make: display.pdu2char()

outputs:
-   domain: stream
    dtype: byte

inputs:
-   domain: message
    id: in

file_format: 1

